// ============================================================
// P5 Continue - Participant Template
// ============================================================
// 以下のメタデータを必ず記入してください / Fill in the metadata below
const TITLE = "作品タイトル / Your Title";
const AUTHOR = "あなたの名前 / Your Name";
// ============================================================
//
// ルール / Rules:
//   - キャンバスサイズ: 400x800（システムが自動設定します。createCanvas()は書かないでください）
//   - フレームレート: 60fps（システムが自動設定します）
//   - 総フレーム数: 600（= 10秒）。frameCount が 600 に達すると自動停止します
//   - 画面上部中央 (200, 0) 付近から「何か」が現れ、
//     画面下部中央 (200, 800) 付近に消えていくアニメーションを作ってください
//   - 「何か」の見た目・動き・演出は完全に自由です
//
// 禁止事項 / Restrictions:
//   - createCanvas() を呼ばないでください
//   - loadImage(), loadSound() 等の外部ファイル読み込みは禁止です
//   - fetch(), XMLHttpRequest 等のネットワーク通信は禁止です
//
// Tips:
//   - frameCount (1〜600) を使って進行度を計算できます
//   - let progress = frameCount / 600; // 0.0 → 1.0
//   - setup() で初期化、draw() でアニメーションを描画してください
//
// ============================================================

function setup() {
  // ここに初期化処理を書いてください
}

function draw() {
  background(0);

  // -- サンプル: 白い円が上から下へ落ちる --
  let progress = frameCount / 600;
  let x = 200;
  let y = progress * 800;

  fill(255);
  noStroke();
  ellipse(x, y, 40, 40);
}
